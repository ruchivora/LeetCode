class Solution {
    public int maxDistToClosest(int[] seats) 
    {
    		int left  = 0 ;
    		int right = 0 ;

        for( int i = 0 ; i < seats.length ; i++ )
        {
        		if( seats[i] == 0 )
        		{
        			if( i - 1 > 0 && ) left = i - 1 ;
        			
        			if( i != seats.length - 1 ) right = right + 1

        		}
        }
    }
}